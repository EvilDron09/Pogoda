extends layout

block content
    .container
        .row
            .col-sm-3
                .input-group.mb-3
                    input.form-control(type="text", placeholder="Ведіть місто", aria-describedby="form_weather", id="cityInput")
                    button.btn.btn-outline-secondary( type="button", id="formWeather") Пошук
            .col-sm-9
                #weather

    div(id="divWeather")
block js
    script(src="/javascripts/main.js")
